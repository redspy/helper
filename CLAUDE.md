# CLAUDE.md

이 저장소는 `Helper` 프로젝트입니다. 웹에서 할 일을 등록하면 지정된 시간에 Gemini가 내용을 정리하고 Telegram으로 전송하는 Node.js 기반 자동화 서버입니다.

이 문서는 Claude Code CLI로 여러 에이전트가 협업할 때 사용하는 공통 운영 규칙입니다. 각 에이전트는 자신의 역할별 가이드와 프롬프트를 함께 참고합니다.

## 프로젝트 기본 정보

- 런타임: Node.js 20 이상
- 서버: Express 4
- 뷰: EJS
- DB: `sql.js` 기반 SQLite 파일 저장
- 스케줄러: `node-cron`
- AI 연동: `@google/genai`
- 메시징: Telegram Bot API
- 주요 실행 명령:
  - `npm install`
  - `npm run setup`
  - `npm start`
  - `npm run dev`

## 에이전트 구성

프로젝트는 네 개 역할의 에이전트가 협업한다.

| 역할 | 핵심 책임 |
|---|---|
| PL | 프로젝트 진행 관리, 히스토리 관리, 의사결정 정리, HTML 슬라이드 장표 업데이트 |
| PM | 업계 동향 분석, 기능 제안, 제품 차별화 포인트 정리, 기획 검수 |
| 개발자 | 기존 코드 이해, 기능 구현, 기존 코드 수정, 유닛테스트 작성, 버그 수정 |
| 품질 | 소스코드와 기능 이해, 테스트 계획/수행, 버그 리포트, 재검증 |

## 역할별 문서

- PL 가이드: `docs/agents/guides/pl-guide.md`
- PM 가이드: `docs/agents/guides/pm-guide.md`
- 개발자 가이드: `docs/agents/guides/developer-guide.md`
- 품질 가이드: `docs/agents/guides/quality-guide.md`
- PL 프롬프트: `docs/agents/prompts/pl-prompt.md`
- PM 프롬프트: `docs/agents/prompts/pm-prompt.md`
- 개발자 프롬프트: `docs/agents/prompts/developer-prompt.md`
- 품질 프롬프트: `docs/agents/prompts/quality-prompt.md`

## 공통 협업 원칙

1. 각 Claude Code CLI 세션은 하나의 역할만 담당한다.
2. 작업 시작 전 `CLAUDE.md`, 본인 역할 가이드, 관련 기획/구현 문서를 먼저 읽는다.
3. 작업 범위가 불명확하면 임의로 크게 확장하지 말고, 현재 목표와 필요한 확인사항을 먼저 정리한다.
4. 기존 코드 스타일과 구조를 우선한다.
5. 민감정보가 포함된 `.env`, API 키, 토큰, 세션 키는 절대 커밋하거나 문서에 노출하지 않는다.
6. 변경사항은 다른 역할이 검토할 수 있도록 파일, 이유, 영향 범위, 검증 결과를 남긴다.
7. 의사결정은 이유와 대안을 함께 기록한다.
8. 구현과 검증 결과는 PL이 히스토리와 슬라이드에 반영할 수 있는 형태로 요약한다.

## 기본 작업 흐름

1. PM이 기능 배경, 사용자 가치, 요구사항, 검수 기준을 정리한다.
2. PL이 작업 범위, 담당 역할, 의사결정, 일정, 산출물을 정리한다.
3. 개발자가 코드 구조를 분석하고 기능을 구현하며 테스트를 작성한다.
4. 품질 에이전트가 테스트 시나리오를 작성하고 기능/회귀 테스트를 수행한다.
5. PM이 기획 의도와 사용자 경험 기준으로 결과를 검수한다.
6. PL이 최종 히스토리, 결정사항, 다음 액션, HTML 슬라이드 장표를 업데이트한다.

## 공통 보고 형식

각 에이전트는 작업 종료 시 아래 형식으로 보고한다.

```md
## 작업 요약
- 역할:
- 목표:
- 수행 내용:

## 변경/산출물
- 파일:
- 문서:
- 테스트:

## 판단 근거
- 주요 결정:
- 고려한 대안:
- 남은 리스크:

## 다음 액션
- PL 확인 필요:
- PM 확인 필요:
- 개발자 확인 필요:
- 품질 확인 필요:
```

## 의사결정 기록 형식

PL은 중요한 결정이 발생할 때 아래 형식으로 정리한다.

```md
## Decision
- 날짜:
- 결정:
- 배경:
- 선택한 이유:
- 고려한 대안:
- 영향 범위:
- 후속 작업:
```

## 버그 리포트 형식

품질 에이전트와 PM은 문제를 발견하면 아래 형식으로 정리한다.

```md
## Bug
- 제목:
- 심각도: Critical / High / Medium / Low
- 재현 절차:
- 기대 결과:
- 실제 결과:
- 영향 범위:
- 참고 파일/화면:
- 제안:
```

## 개발 공통 규칙

- 서버 코드는 현재 CommonJS 스타일을 유지한다.
- 기존 라우터, 서비스 모듈, EJS 뷰 구조를 우선 활용한다.
- DB 스키마 변경이 필요하면 `migrations/`에 새 마이그레이션을 추가한다.
- 시간 관련 로직은 `TZ=Asia/Seoul` 기준 동작을 고려한다.
- 외부 API 호출은 실패, 타임아웃, 재시도, 로그 기록 관점에서 검토한다.
- UI 변경 시 로그인, 할 일 등록, 반복 실행, 지난 할 일 복구 흐름을 함께 확인한다.
- 구현이 끝나면 가능한 범위에서 테스트 또는 실행 검증 결과를 남긴다.

## 산출물 기준

작업이 완료되었다고 판단하려면 최소한 아래 조건을 만족해야 한다.

- 요구사항 또는 이슈의 목표가 명확히 충족됨
- 변경된 파일과 이유가 설명됨
- 테스트 또는 검증 결과가 기록됨
- 남은 리스크와 후속 작업이 있으면 명시됨
- 다른 역할이 이어서 작업할 수 있는 인계 정보가 있음
